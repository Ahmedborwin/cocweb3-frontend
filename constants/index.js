const GamecontractAddress = require("./GamecontractAddress.json")
const Gameabi = require("./Gameabi.json")
const LootcontractAddress = require("./LootcontractAddress.json")
const Lootabi = require("./Lootabi.json")
const providerURLs = require("./providerURLs.json")

module.exports = {
    GamecontractAddress,
    Gameabi,
    LootcontractAddress,
    Lootabi,
    providerURLs,
}
